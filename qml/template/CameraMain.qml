import QtQuick 2.9
import QtQuick.Controls 2.2

Pane {
    Label{
        wrapMode: Text.WordWrap
        text:qsTr("<h1>This is your Camera playground</h1>
Just empty this file and replace with your qml code...<br>
<h2>what could be done ?</h2>
<ul>
<li>add a camera
<li>Display a view finder, capture an image
<li>Change settings : zoom, camera location, flash
<li>add a delay capture feature
<li>...
</ul>
")
    }
}
